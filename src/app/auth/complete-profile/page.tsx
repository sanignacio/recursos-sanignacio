import { CompleteProfileForm } from "~/components/auth/complete-profile";

export const metadata = {
  title: "Completa tu perfil",
};

export default function ForgotPasswordPage() {
  return <CompleteProfileForm />;
}
